# CardsLite PHP

Telegram бот для знакомства через мини-игры с использованием [telegram-bot-sdk](https://github.com/irazasyed/telegram-bot-sdk).

## 🚀 Технологический стек

- **PHP**: 8.1+
- **Telegram Bot SDK**: irazasyed/telegram-bot-sdk v3.15
- **База данных**: SQLite (PDO)
- **Режим работы**: Long Polling
- **Конфигурация**: .env файл (phpdotenv)

## 📁 Структура проекта

```
CardsLitePHP/
├── bot.php              # 🚀 Точка входа и основные обработчики
├── src/
│   ├── Database.php     # Работа с SQLite базой данных
│   ├── Utils.php        # Вспомогательные функции
│   └── UI.php           # Клавиатуры и форматирование сообщений
├── composer.json        # Зависимости проекта
├── .env.example         # Пример конфигурации
├── .gitignore          # Исключения для Git
└── README.md           # Документация
```

## ⚙️ Установка и запуск

### 1. Установите зависимости

```bash
cd CardsLitePHP
composer install
```

### 2. Настройте окружение

Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и укажите:

```env
BOT_TOKEN=your_telegram_bot_token
ADMIN_ID=your_telegram_user_id
```

**Где взять:**
- `BOT_TOKEN` - получите у [@BotFather](https://t.me/BotFather)
- `ADMIN_ID` - узнайте свой ID у [@userinfobot](https://t.me/userinfobot)

### 3. Запустите бота

```bash
php bot.php
```

Вы должны увидеть:
```
✓ База данных инициализирована
🤖 Бот запущен
```

## 🎮 Игровой процесс

1. **Игрок 1** создает комнату (`/create_room <topic_id>`) → получает `room_id`
2. **Игрок 2** присоединяется (`/join_room <room_id>`) → игра начинается
3. Оба получают вопрос #1
4. Каждый отвечает → ответы раскрываются после ответов обоих
5. Оба нажимают `▶️ Далее` → показывается вопрос #2
6. Повторяется до конца вопросов
7. Игра завершается → комната удаляется

## 📝 Команды бота

### Пользовательские команды

- `/start` - Главное меню с приветствием
- `/topics` - Показать все доступные темы
- `/create_room <topic_id>` - Создать новую комнату
- `/join_room <room_id>` - Присоединиться к комнате (или без параметра для ввода ID)
- `/stop` - Выйти из текущей игры

### Админские команды

- `/add_topic` - Создать новую тему с вопросами (только для ADMIN_ID)

**Формат добавления вопросов:**
```
1.Первый вопрос здесь
2.Второй вопрос здесь
3.Третий вопрос здесь
```

## 🗄️ База данных

**Схема SQLite (cardslite.db):**

- `topics` - темы для игр
- `questions` - вопросы по темам
- `rooms` - игровые комнаты
- `answers` - ответы игроков
- `chat_messages` - история чата между игроками

База данных создается автоматически при первом запуске.

## 🔧 Разработка

### Основные классы

**Database.php** - статические методы для работы с БД:
- `initDb()` - инициализация схемы
- `createRoom()`, `joinRoom()` - управление комнатами
- `saveChatMessage()`, `getChatMessages()` - чат между игроками
- `moveToNextQuestion()` - переход к следующему вопросу

**UI.php** - генерация клавиатур и форматирование:
- `getMainMenuKeyboard()` - главное меню
- `getTopicSelectionKeyboard()` - выбор темы
- `formatChatHistory()` - история переписки

**Utils.php** - утилиты:
- `parseQuestions()` - парсинг вопросов из текста
- `isAdmin()` - проверка прав администратора

### Архитектура

Бот использует **Long Polling** режим с основным циклом в `bot.php`. Все обработчики вызываются синхронно.

**Состояния пользователей** хранятся в памяти (`$userStates`). Для production рекомендуется использовать Redis или БД.

## 🚀 Деплой

### Локальный запуск

```bash
php bot.php
```

Бот будет работать в режиме long polling. Держите процесс активным.

### Запуск в фоне (Linux)

```bash
nohup php bot.php > bot.log 2>&1 &
```

### Остановка бота

```bash
# Найдите процесс
ps aux | grep bot.php

# Остановите по PID
kill <PID>
```

### Production рекомендации

1. **Используйте supervisor** для автоматического перезапуска
2. **Настройте логирование** (Monolog)
3. **Используйте Redis** для хранения состояний
4. **Включите error reporting** только для dev окружения